# Bindings
bind k killw
set-option -g focus-events on
set -g mouse on
bind m set -g mouse on \; display 'Mouse: ON'
bind M set -g mouse off \; display 'Mouse: OFF'
set-option -ga terminal-overrides ",xterm-256color:Tc"
# Prefix + p: Open a quick, centered floating terminal
bind p display-popup -w 80% -h 80% -E
bind f display-popup -w 85% -h 85% -E "tmux attach -t float || tmux new -s float"
bind j display-popup -w 95% -h 85% -E "watch -c -n 1 'jruns; jqueue;'"
bind g display-popup -w 95% -h 85% "gc"

# Pane borders (yellow, matching theme)
set -g pane-border-style "fg=#ffea02"
set -g pane-active-border-style "fg=#ffea02"

# Set a border style (options: single, double, heavy, rounded, none)
set -g popup-border-style "fg=#ffea02"
set -g popup-border-lines "double"
# Prefix + h: Quick system monitor (closes when you press 'q' in htop)
bind h display-popup -w 80% -h 70% -E "htop"

bind '*' select-pane -m
bind § swap-pane -s '{marked}'
# Coloring
#set -g default-terminal "xterm-256color"
# 256-color + truecolor
set -g default-terminal "tmux-256color"
set -as terminal-overrides ',*:Tc'   # or: set -as terminal-features ',xterm-256color:RGB'

# Forward applications' OSC 52 clipboard writes to the outer terminal (Windows Terminal)
set -g set-clipboard on
set -as terminal-features ',*:clipboard'

# --- Session chooser / copy-mode selection highlight ---
set -g mode-style "fg=#000000,bg=#ffea02,bold"

# --- Primary and secondary colors (defined here, not from shell env) ---

# --- Status Bar General ---
set -g status-interval 1
set -g status-position bottom
set -g status-style "bg=#ffea02"

# --- Command prompt (prefix+:): blue status/message line ---
set -g message-style "fg=#000000,bg=#03a1fc,bold"
set -g message-command-style "fg=#000000,bg=#03a1fc,bold"

# --- LEFT SIDE: #I then all sessions in tmux order (same as prefix-s); current=blue, others=blue slant + yellow ---
set -g status-left-length 120
set -g status-left "#[fg=#ffea02,bg=#000000]#[fg=#ffffff,bg=#000000,bold] #I #[fg=#000000,bg=#ffea02]#(~/.tmux/other-sessions.sh)"

# --- WINDOW LIST: Center ---
setw -g window-status-format ""  #[fg=#ffea02,bg=#ffea02] "
setw -g window-status-current-format "" #[fg=#ffea02,bg=#000000]#[fg=#ffffff,bg=#000000,bold] #[fg=#000000,bg=#ffea02]"

# --- RIGHT SIDE: Slurm + Time + Date ---
set -g status-right-length 150
set -g status-right "#[fg=#ffea02,bg=#000000]#[fg=#ffffff,bg=#00000t0] #(~/.tmux/slurm-status.sh) #[fg=#000000,bg=#ffea02]#[fg=#000000,bg=#ffea02,bold] %H:%M #[fg=#000000,bg=#ffea02,bold] %Y-%m-%d #[fg=#ffea02,bg=#000000] "

# --- RELOAD BINDING ---
bind r source-file ~/.tmux.conf \; display "Config reloaded"